# behavior_Master.Box.Out.ls — Award-box button

Seven lines: a `mouseUp` handler that dismisses the inter-level award box
(`master_obj.getOut()`, `award_obj.dropBox()`).

**Swift counterpart:** none needed — the remake has no award-box intermission; level
transitions are immediate. Pure UI glue with no simulation logic.

## Verdict

Nothing to compare or port.
